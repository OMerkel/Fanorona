# Use PowerShell as the npm script shell.
# This allows scripts to prepend the correct Node.js path before HPC's 'node' command.
# Required on machines where 'node' in PATH resolves to Microsoft HPC Pack instead of Node.js.
script-shell=C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe
